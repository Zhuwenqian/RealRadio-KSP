# 功能更新

## 2026-07-04
- 初始化 KSP 1.12.5 模组开发环境。
- 创建 Visual Studio 解决方案 `RealRadio.sln` 与 MSBuild 项目 `RealRadio.csproj`。
- 配置 KSP/Unity DLL 引用，全部使用相对路径指向 `KSP DLL/` 目录。
- 创建基础 Mod 入口类 `Source/RealRadioMod.cs`，使用 `[KSPAddon]` 在主菜单启动时初始化。
- 配置编译后自动将 `RealRadio.dll` 复制到 `GameData/RealRadio/Plugins/`。
- 添加 `GameData/RealRadio/RealRadio.version` 版本文件。
